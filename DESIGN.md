# Koma design system

状态：**已采用**。当前公共界面采用柔和黑白灰的 **Koma 视频小助手（Video Companion）** 方向。本文件定义设计与验收要求，不代表所有浏览器检查已经完成。本轮范围见 [2026-09-07 界面改版计划](docs/plans/2026-09-07-companion-ui-refresh.md)；[2026-08-24 Frame Atelier 计划](docs/plans/2026-08-24-frame-atelier-design.md) 仅作历史参考，不再决定当前公共界面和人物。

## 目的

Koma 是 AI 视频分析网站，帮助用户从视频中获得总结、章节、关键帧、字幕、结构化数据和可下载文件。界面应让人轻松放入视频、说明要求、查看结果，再找到想重看的时刻。角色提供亲和力，工作内容保持清楚、准确、可读。

## 产品文案

- 首页、页头与 README 明确表达视频分析用途，围绕“分析视频、查看结果、定位片段、提取数据、导出结果”组织说明。
- 保留亲切、简短的语气。角色可以招呼用户、说明下一步；“笔记”只用于具体的输出用途，不概括整个产品或替代分析流程名称。
- 来源、分析要求、结果的流程文案使用“放入视频 → 告诉我要求 → 查看分析”；导出按钮说明实际导出的分析结果或字幕。
- README 默认使用英文，页首保留简体中文切换；中文版提供返回英文的链接。两版能力、登录要求、配置前提与隐私边界保持一致。
- 仓库入口链接到 `https://github.com/yuxino/koma/`，使用“Star on GitHub”或“在 GitHub 点个 Star”等清楚文案；与“通过 GitHub 登录”分别命名，不混淆访问仓库和账号登录。

## 视觉概念

- 公共界面以白色、柔和黑色和中性灰建立层级，通过排版、间距和细线区分内容，不使用青柠或其他彩色品牌强调。
- 当前角色参考 Kiri 新版近景的圆脸、大眼与柔和线条：奶白单高马尾、一个黑色蝴蝶结、明亮灰棕眼睛，穿米白毛衣与黑色背带裙，双手抱着黑白场记板。画面聚焦脸和肩部。
- 角色采用圆脸、大眼与柔和线条的动漫近景表达，保持自然微笑和完整的日常衣着。肤色与淡粉腮红是少量自然暖色，不扩展为界面配色。
- 人物、文字气泡与简短招呼只辅助介绍、等待和空状态，不遮挡表单、视频、字幕或结果，也不暗示对话机器人等未实现能力。
- 时间码、播放位置和章节编号继续服务于真实内容；不以装饰取景框、密集英文标签或大幅标题替代产品结构。

## 公共界面令牌

| 角色 | 值 | 用法 |
| --- | --- | --- |
| Paper | `#fafafa` | 页面背景 |
| Paper 2 | `#f1f1f0` | 次级浅灰区域 |
| Surface | `#fff` | 表单、菜单与内容表面 |
| Ink | `#252525` | 主要文字 |
| Ink 2 | `#5e5e5e` | 次要正文 |
| Line | `#e5e5e3` | 内容与控件分隔 |
| Line strong | `#c9c9c6` | 需要更清楚区分的边界 |
| Primary action | `#30302e` | 深色主要按钮与导航选中项，配白字 |
| Focus | `#555` | 键盘焦点轮廓 |

公共界面的选中、焦点与进度使用中性灰或深色；错误、警告继续使用有明确语义的状态色。状态必须同时有文字或图标，不只靠颜色。视频画面可保留深色背景，不把整页改为暗色主题。

## 排版与形状

- 公共界面的标题和正文均使用正常比例的 `Avenir Next`、`PingFang SC`、`Hiragino Sans GB` 等系统无衬线字体；不使用窄体展示字或压缩字形。
- 时间码与必要元数据可使用 `SFMono-Regular`、`Roboto Mono` 和等宽回退；普通说明不以等宽大写字替代正文。
- 正文至少 14px；9–11px 仅用于非关键标签和时间码。标题保持合理字号与行高，以完整内容和自然换行为先。
- 使用 4px 基础间距，常用阶梯为 4、8、12、16、20、24、32、40、48、64px。
- 主导航、主要按钮与短选项允许胶囊形状；表单与弹窗使用柔和圆角，避免每个小内容都单独包成卡片。
- 主要靠留白、字号、字重和细分隔线建立层级；不使用重阴影、玻璃效果或渐变制造层次。

## 页面结构

- 页头：在桌面与移动端保留醒目的 GitHub / Star 仓库入口，支持键盘操作且不挤压主要分析动作；首页的欢迎区也应容易找到仓库入口。
- 未登录：一个完整欢迎区，包含用途说明、GitHub 登录入口与新角色；下方用三条具体能力说明摘要、定位和导出，不放虚构视频结果。
- 已登录工作区：紧凑欢迎区位于表单上方。桌面表单左列放视频来源，右列放分析要求；共同的底部操作区负责开始分析。移动端改为来源、要求、开始的单一阅读顺序。
- 分析要求：课程、访谈/会议与产品预设直接填入可编辑内容；高级设置、JSON 草稿确认、字段解释和默认配置入口继续可用。
- 资料库：独立导航入口与逐行任务列表，以标题、摘要、日期和状态组织记录；关键词搜索和状态筛选保持可见，不改为重复卡片墙。
- 进度页：展示任务名称、百分比、阶段和真实服务端详情；失败原因清楚可读，仅在来源可恢复时提供直接重试。
- 结果页：完整显示标题，允许自然换行，不截断为单行省略号；视频与字幕保持平衡布局，摘要使用浅色表面与正常正文，检索、时间跳转和导出操作容易找到。
- 弹窗：高级设置、帮助、画面预览和任务详情保留一致的标题、关闭、滚动和焦点规则；内容多时在弹窗内滚动。
- 移动端：控制角色占用，优先保证任务操作与内容阅读；结果改为单列，不得产生页面级水平滚动。

## 后台边界

本轮 `/admin` 只更换新角色图片。后台保留原有排版、控件密度、主题与状态语言，不套用公共界面的黑白灰令牌、胶囊导航或欢迎布局。公共界面样式应限定在其容器内，避免污染后台；角色继续只用于后台品牌与登录入口。

## 交互与可访问性

- 动效以透明度和小幅位移为主，交互反馈 120–240ms，列表入场不超过 360ms；角色悬停可小幅倾斜。不使用妨碍操作的弹跳、视差或装饰性循环动画。
- 支持 `prefers-reduced-motion`，关闭非必要动画、位移和滚动过渡。
- 所有交互元素必须有清晰的 hover、active、disabled 和 `:focus-visible` 状态。
- 正文应达到 WCAG AA 4.5:1；大字号文字和必要控件边界至少 3:1。以下要求需要实际测量与交互验收，不能用令牌表代替结果。
- 键盘可以完成 GitHub 登录入口、主导航、上传选择、模式切换、预设选择、资料库筛选、字幕检索、弹窗、关键帧切换和结果跳转。
- 触控目标至少 44×44px；错误使用文本和 `role="alert"`，异步进度使用可感知的状态说明。
- 装饰性角色图使用空 `alt`；关键帧和有信息意义的图像保留描述。

## 禁止项

- 不使用渐变 Hero、彩色大背景、玻璃拟态、发光球或装饰网格。
- 不用成排卡片代替真正的信息结构，也不制作假的浏览器或剪辑软件外壳。
- 不把角色、状态色或历史青柠色扩张成公共界面的品牌主题。
- 不使用虚构指标、社交证明、能力或处理状态。
- 不让巨型营销标题、角色插图或阴影抢过分析工作台。
- 不把旧人物图、旧生成提示词或历史计划重新当作本轮角色要求。

## 产品契约

GitHub 登录后的新任务归属账号，跨浏览器恢复；结果与文件由服务端检查本人或管理员权限。旧浏览器身份只用于显式找回本浏览器记录，不自动认领或扩大历史内容的访问范围。认领不能收回已经下载或缓存的副本。

必须保留双语界面、上传与链接输入、来源校验、高级提取的草稿确认、字段解释、任务进度、Provider 配置、文件下载、播放器同步和独立管理员权限。AI 生成的草稿必须先审阅再应用，旧响应不能覆盖新草稿；字段解释跟随有效 JSON 结构。资料库检索、字幕检索与定位、失败重试、Markdown/SRT 导出以及账号隔离的本地草稿和默认配置不因视觉改版退回。旧浏览器草稿只在用户明确选择后载入当前账号，不自动迁移。

本轮视觉改版不增加服务端能力，也不更换 Provider 配置。API 及迁移约定见 [GitHub 工作区决策](docs/decisions/0003-github-workspaces.md)。

当前角色资产为 [`public/koma-ponytail-portrait.webp`](public/koma-ponytail-portrait.webp)，采用极浅中性实底的原生近景。图片以完整近景配小圆角展示。小尺寸品牌头像使用用户指定图片的圆形裁切版本 [`public/koma-logo-round.png`](public/koma-logo-round.png)，为 512px PNG，圆外具有真实透明通道；浏览器图标使用同款 64px [`public/koma-favicon-round.png`](public/koma-favicon-round.png)。页面插画与 Logo 的用途分开，不再把全身图放大裁切。页面插画的生成参考、实际尺寸与完整提示词见[角色素材说明](docs/visual-assets.md)。

Logo 的用户指定原图、圆形透明裁切、尺寸与使用范围见 [Logo 素材说明](docs/logo-assets.md)。
